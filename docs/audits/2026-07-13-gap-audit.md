# Gap audit — 2026-07-13

Source: three-agent repo sweep (code inventory / policy-doc vision extraction / delivery
track record), cross-checked by hand. Operational gaps only — the capability-level audit
(analysis quality, markout validation, regime engine) is tracked separately in
`2026-07-13-capability-audit.md`.

## P0 — flywheel automation is silently down

| # | Gap | Evidence |
|---|-----|----------|
| 1 | Daily cron scan broken & unmonitored — 45 tracebacks in `~/.config/option-wizard/daily.log`; last run (2026-07-10) died on `httpx.ReadTimeout` in `XenonClient._get("/portfolio")`. No failure alerting; errors only reach the log file. | `daily.log`, `scripts/_clients/xenon.py` |
| 2 | CI never runs the test suite — `.github/workflows/skill-lint.yml` runs skill-lint only; 491 unit tests + ruff are local-only. A PR can merge with a red suite. | `.github/workflows/skill-lint.yml` |
| 3 | Repo hygiene: `uv.lock` untracked (fresh clone unpinned); `AGENTS.md` untracked and missing hard rules 9–10; `docs/superpowers/plans/2026-07-06-signal-lab.md` belongs to `~/projects/signal-lab`, not this repo. | `git status` |

Fix: xenon retry + failure-email in `manage_positions` (reuse `email_sender.py`); add
pytest+ruff job to CI; hygiene 三件套 in one PR.

## P1 — doc drift (rules docs ARE runtime code for an LLM-orchestrated skill)

1. `SKILL.md:168` still instructs annotating "$20 strike-width limit misfires" — bug fixed
   in PR #29 (`defined_risk_audit.py` now uses quantity-conservation bucket check).
2. `analysis-runbook.md:17-74` L0 still says IB MCP primary — xenon migration (2026-06-18)
   never propagated to the most-used runbook.
3. `README.md` never mentions xenon; describes pre-migration architecture.
4. Workflow count inconsistent: "4" (`SKILL.md:139`, `workflows-overview.md:3`), "6"
   (`workflows-overview.md:11`), actual 7 (W1/W2a/W2b/W3/W4/W5/W6).
5. AQ/DQ refusal red lines: summary docs say 6 (`SKILL.md:78`), actual is 7 (R0–R6,
   `aq-dq-framework.md:153-165`).
6. `fair_aq_dq.py:259` stale "stubs" comment above 1,400 fully-implemented lines.
7. `CHANGELOG.md` stops at 0.2.0 (2026-06-23); 6 major features since unlogged.
8. `macro-hedge-convexity.md:304-311` open-questions says HY OAS ingestion not wired —
   `_clients/fred.py:88 hy_oas_signal()` exists.

Fix: one doc-coherence PR touching all 8.

## P2 — 复盘 engine is a Phase 1 scaffold (blocks the quantitative endgame)

1. `retrospective.py` CLI live fetchers not wired (self-declared scaffold at `main()`);
   trader must hand-feed `run_review()`.
2. Closing-trade pairing unimplemented (`review-framework.md:712`) — opens never attributed
   to realized P/L.
3. N ≥ 50 calibration gate (±2% → ±0.5σ) unreachable while 1–2 hold.
4. Pitfall promotion pipeline stalled: 2 drafts in `_drafts/` since 2026-06-04; monthly
   skill audit found Δ3/Δ4/Δ6 improvement items not landed.

## P3 — deferred features (build when triggered)

- `evaluate_placed_aq` DQ mirror (`fair_aq_dq.py:1550` NotImplementedError) — when a real
  DQ quote arrives.
- Structural check for margin-secured IB short puts (hard-rule-#1 enforcement hole, audit
  item R2).
- xenon multi-expiry collapse (diagonals/calendars flatten,
  `xenon_normalize.py` docstring) — when trading multi-expiry structures.
- `backtest_index_premium.py` (v1.1) — after N ≥ 10 entry-timing calibration data exists.
- Multi-currency P&L aggregation weakness (audit item C3).
